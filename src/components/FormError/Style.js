import { StyleSheet } from 'react-native';
import { regularFamily } from '@constants/fonts';

const styles = StyleSheet.create({
  errorText: {
    fontFamily: regularFamily,
    fontSize: 14,
    fontWeight: 'normal',
  },
});

export default styles;
